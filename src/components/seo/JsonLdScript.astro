---
interface Props {
  schema?: unknown;
}

const { schema } = Astro.props as Props;

if (!schema) {
  return null;
}

const payload = typeof schema === 'string' ? schema : JSON.stringify(schema);
---

<script type="application/ld+json" is:inline set:html={payload} />
